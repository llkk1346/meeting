<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="utf-8">
    <!-- 위 3개의 메타 태그는 *반드시* head 태그의 처음에 와야합니다; 어떤 다른 콘텐츠들은 반드시 이 태그들 *다음에* 와야 합니다 -->
    <title><%= title%></title>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <!-- 합쳐지고 최소화된 최신 CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">

    <!-- 부가적인 테마 -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css">

    <!-- 합쳐지고 최소화된 최신 자바스크립트 -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
    <!-- fullcalender -->
    <link href='https://cdn.jsdelivr.net/npm/fullcalendar@5.8.0/main.min.css' rel='stylesheet' />
    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@5.8.0/main.min.js'></script>
    <!-- fullcalendar 언어 CDN -->
    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@5.8.0/locales-all.min.js'></script>



    <style>
        /* body 스타일 */
        html, body {
            overflow: hidden;
            font-family: Arial, Helvetica Neue, Helvetica, sans-serif;
            font-size: 14px;
        }
        /* 캘린더 위의 해더 스타일(날짜가 있는 부분) */
        .fc-header-toolbar {
            padding-top: 1em;
            padding-left: 1em;
            padding-right: 1em;
        }
    </style>
</head>

<body>
    <% if(findUserId === undefined){%>
        <div class="container theme-showcase" role="main">
            <div><h3> 손님 </h3></div>
            <div id="calendar-container">
                <div id="calendar"></div>
            </div>
        
        </div> <!-- /container -->
    <%}%>
    <% if(findUserId !=undefined){%>
        <div class="container theme-showcase" role="main">
            <div><h3> <%= findUserId%> 님</h3></div>
            <div id="calendar-container">
                <div id="calendar"></div>
            </div>
        
        </div> <!-- /container -->
    <%}%>   


<!-- Button trigger modal -->
<div id=myModalDiv>
<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter" id="myModals">
</button>
</div>
  <!-- Modal -->
  <div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLongTitle">회의실 예약</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
        <form id='meetingForm'>
            <div class="row">
                <div class="col-sm-12">
                    <label class="meetingRoom"></label>
                    <select class="form-control" id="meetingRoom" name="meetingRoom">
                        <option value="0">회의실 목록</option>
                        <%
                            for(let i=0; i<meetingRoom.length; i++){
                                let room = meetingRoom[i]; 
                        %>
                        <option value= <%=room.id%>><%=room.room_name%> (최대인원수 : <%=room.count%>)</option>
                        <%}%>
                    </select>
                </div>
            </div>    
            <div class="row">
                <div class="col-sm-12">
                    <label class="meetingTitle"></label>
                    <input type="text" class="form-control" id="meetingTitle" name="meetingTitle" placeholder="회의제목"> 
                </div>
            </div>
            <br>
            <div class="row">
                <div class="col-sm-6">
                    <input type="date" class="form-control" id="meetingDateStartYMD" name="meetingDateStartYMD"> 
                </div>
                
                <div class="col-sm-3">
                    <select class="form-control" id="meetingStartTimeY" name="meetingStartTimeY">
                        <%
                            for(let i=8; i<20; i++){
                        %>
                        <option value= <%=i%>><%=i%>시 </option>
                        <%}%>
                    </select> 
                </div>
                <div class="col-sm-3">
                    <select class="form-control" id="meetingStartTimeM" name="meetingStartTimeM">
                        <%
                            for(let i=0; i<61; i+=10){
                        %>
                        <option value= <%=i%>><%=i%>분 </option>
                        <%}%>
                    </select>
                    
                </div>
            </div>
            <br>
            <div class="row">
                <div class="col-sm-6">
                    <input type="date" class="form-control" id="meetingDateEndYMD" name="meetingDateEndYMD">
                </div>
                <div class="col-sm-3">
                    <select class="form-control" id="meetingEndTimeY" name="meetingEndTimeY">
                        <%
                            for(let i=8; i<20; i++){
                        %>
                        <option value= <%=i%>><%=i%>시 </option>
                        <%}%>
                    </select> 
                </div>
                <div class="col-sm-3">
                    <select class="form-control" id="meetingEndTimeM" name="meetingEndTimeM">
                        <%
                            for(let i=0; i<61; i+=10){
                        %>
                        <option value= <%=i%>><%=i%>분 </option>
                        <%}%>
                    </select>
                    
                </div>
            </div>
            <div class="row">
                <div class="col-sm-12">
                    <label class="meetingContent"></label>
                    <textarea id="meetingContent" name="meetingContent" class="form-control"></textarea>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-12">
                    <label></label>
                    <select class="form-control" id="peoples" name="peoples">
                        <option value="0">인원</option>
                        <%
                            for(let i=1; i<21; i++){
                        %>
                        <option value=<%=i%>><%=i%>명</option>
                        <%}%>
                    </select>
                </div>
            </div>
            <input type="hidden" id="userName" name="userName" value=<%=findUserUID%>>
            <input type="hidden" id="peopleCount" name="peopleCount">
        </form>    
        </div>
        
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">취소</button>
          <button type="button" id="checkSave" class="btn btn-primary" onclick="saveMeeting()">예약</button>
        </div>
      </div>
    </div>
  </div>
</body>

<script>
// 수정 저장 구분 플래그 0 =저장 1 = 수정
var updateFlag = 0;
//업데이트할 user_id 값
var id;


    (function(){
        $(function(){
                // calendar element 취득
                var calendarEl = $('#calendar')[0];
                // full-calendar 생성하기
                var calendar = new FullCalendar.Calendar(calendarEl, {
                    height: '700px', // calendar 높이 설정
                    expandRows: true, // 화면에 맞게 높이 재설정
                    slotMinTime: '08:00', // Day 캘린더에서 시작 시간
                    slotMaxTime: '20:00', // Day 캘린더에서 종료 시간
                    // 해더에 표시할 툴바
                    // title : 현재날짜 (월/주/일) / prev: 달력을 (월/주/일) 전으로 이동 / next: 달력을 (월/주/일) 후로 이동
                    // / prevYear: 달력을 1년 전으로 이동 / nextYear : 달력을 1년 후로 이동 / today: 현재날짜 / a view name: 사용가능한 캘린더로 전환(캘린더 view 이름)
                    headerToolbar: {
                        left: 'prev,next today',
                        center: 'title',
                        right: 'dayGridMonth,timeGridWeek,timeGridDay,listWeek'
                    },
                    initialView: 'dayGridMonth', // 초기 로드 될때 보이는 캘린더 화면(기본 설정: 달)
                    //initialDate: '2021-07-15', // 초기 날짜 설정 (설정하지 않으면 오늘 날짜가 보인다.)
                    navLinks: true, // 날짜를 선택하면 Day 캘린더나 Week 캘린더로 링크
                    editable: true, // 수정 가능?
                    selectable: true, // 달력 일자 드래그 설정가능
                    nowIndicator: true, // 현재 시간 마크
                    dayMaxEvents: true, // 이벤트가 오버되면 높이 제한 (+ 몇 개식으로 표현)
                    locale: 'ko', // 한국어 설정
                    //hiddenDays:[0,6],
                    eventAdd: function(obj) { // 이벤트가 추가되면 발생하는 이벤트
                        console.log(obj);
                    },
                    eventChange: function(obj) { // 이벤트가 수정되면 발생하는 이벤트
                        console.log(obj);
                    },
                    eventRemove: function(obj){ // 이벤트가 삭제되면 발생하는 이벤트
                        console.log(obj);
                    },
                    select: function(arg) { // 캘린더에서 드래그로 이벤트를 생성할 수 있다.
                        updateFlag = 0;
                        $('.modal').on('hidden.bs.modal', function(e){
                            $(this).find('form')[0].reset();
                        })
                        let meetingStart = document.getElementById("meetingDateStartYMD");
                        let meetingEnd = document.getElementById("meetingDateEndYMD");

                        //시작일 하루 밀리는 거 방지 하기위함.
                        meetingStart.value = new Date(arg.start.getTime() - (arg.start.getTimezoneOffset() * 60000)).toISOString().slice(0,10);
                        meetingEnd.value = arg.end.toISOString().slice(0,10);
                        console.log("D_D : ",new Date().toISOString().slice(0,10));
                        console.log("A_A : ",new Date(arg.start.getTime() - (arg.start.getTimezoneOffset() * 60000)).toISOString().slice(0,10));
                        //이전날짜 클릭금지.
                        if(new Date(arg.start.getTime() - (arg.start.getTimezoneOffset() * 60000)).toISOString().slice(0,10) < new Date().toISOString().slice(0,10)){
                            alert("이전날짜는 클릭할수 없습니다.");
                            return false;
                        }
                        document.getElementById('myModals').click();
                    },
                    // 이벤트
                    // title: '' -> String 이벤트에 표시될 제목
                    // start: '' -> ex) '2022-01-04' Date object, 이벤트 시작 날짜(& 시간)
                    //              ex) '2022-01-04T16:00:00'
                    // end: '' -> ex) '2022-01-04' Date object, 이벤트 종료 날짜(& 시간)
                    events: function(info, succcessCallback, failureCallback){
                        $.ajax({
                            type: "GET",
                            url: "/event/search",
                            dataType: "json",
                            success: function(data){
                                let list = data;
                                //document.getElementById('peoplesCount').value;
                                let backColor;
                                let events = list.map(function(item){
                                    if(item.room_name == 1){
                                        backColor = '#5C6AC4';
                                    } else if( item.room_name ==2){
                                        backColor = '#FC4700'
                                    }else if( item.room_name ==3){
                                        backColor = '#7DCD00'
                                    }else if( item.room_name ==4){
                                        backColor = '#770055'
                                    }
                                    return{
                                        groupId: item.user_name,
                                        title: item.title,
                                        start: item.start_date.slice(0,19),
                                        end: item.end_date.slice(0,19),
                                        backgroundColor: backColor
                                    }
                                })
                                console.log("events : ",events);
                                succcessCallback(events);
                            },
                            error: function(error){
                                console.log(error);
                            }
                        });
                    },
                    eventClick:function(events){
                        let startDate = new Date(events.event.start);
                        let endDate = new Date(events.event.end);
                        startDate = dateFormat(startDate);
                        endDate = dateFormat(endDate);

                        $.ajax({
                            type: "POST",
                            url: "/event/reservation/"+events.event.groupId,
                            data: {
                                "dbStartDate" : startDate,
                                "dbEndDate" : endDate
                            },
                            success: function(result){
                                console.log(result);
                                let startH  =""+result.start_date.slice(11).substring(0,2); //예약한 시간 ex ) 08:00:00 => slice(11)의 결과 여기서 substring으로 앞 2글자 빼온다.
                                let startM = ""+result.start_date.slice(14).substring(0,2); //예약한 시간 ex ) 00:00 => slice(14)의 결과 여기서 substring으로 앞 2글자 빼온다.
                                
                                let endH  =""+result.end_date.slice(11).substring(0,2);
                                let endM = ""+result.end_date.slice(14).substring(0,2);
                                
                                if(startH.substring(0,1)==="0"){
                                    startH = startH.substring(1,2);
                                }
                                if(startM.substring(0,1)==="0"){
                                    startM = startM.substring(1,2);
                                }
                                if(endH.substring(0,1)==="0"){
                                    endH = endH.substring(1,2);
                                }
                                if(endM.substring(0,1)==="0"){
                                    endM = endM.substring(1,2);
                                }
                                //예약정보 조회결과 클릭시 타이틀에 값넣어주기
                                document.getElementById("meetingTitle").value = result.title;
                                //예약정보 조회결과 클릭시 예약한 회의실 값으로 변경
                                $("#meetingRoom").val(result.room_name).attr("selected", "selected");
                                //예약정보 조회결과 클릭시 예약시작한 날짜 로 셋팅
                                //ex ) result.start_date => ex) 2022-01-01 08:00:00
                                document.getElementById("meetingDateStartYMD").value = result.start_date.slice(0,10); 
                                //예약정보 조회결과 클릭시 예약끝나는 날짜 로 셋팅
                                document.getElementById("meetingDateEndYMD").value = result.end_date.slice(0,10);
                                
                                //예약정보 조회결과 클릭시 예약한 날짜 시작시간 세팅
                                $("#meetingStartTimeY").val(startH).attr("selected", "selected");
                                //예약정보 조회결과 클릭시 예약한 날짜 시작분 세팅
                                $("#meetingStartTimeM").val(startM).attr("selected", "selected");
                                //예약정보 조회결과 클릭시 예약끝난 날짜 끝시간 세팅
                                $("#meetingEndTimeY").val(endH).attr("selected", "selected");
                                //예약정보 조회결과 클릭시 예약끝난 날짜 끝분 세팅
                                $("#meetingEndTimeM").val(endM).attr("selected", "selected");
                                //예약정보 조회결과 클릭시 입력한 회의내용 값 세팅
                                document.getElementById("meetingContent").value = result.content;
                                //예약정보 조회결과 클릭시 입력한 회의인원 수 세팅
                                $("#peoples").val(result.count).attr("selected", "selected");
                                
                                //수정인지 저장인지 구분하는 플래그.
                                updateFlag = 1;
                                //수정할 예약정보
                                id= result.id;
                                
                                //글작성자 확인
                                let checkUser = document.getElementById('userName').value;
                                //modal창 오픈
                                if(result.user_name==checkUser){
                                    document.getElementById('meetingTitle').disabled = false;
                                    document.getElementById('meetingRoom').disabled = false;
                                    document.getElementById('meetingDateStartYMD').disabled = false;
                                    document.getElementById('meetingDateEndYMD').disabled = false;
                                    document.getElementById('meetingStartTimeY').disabled = false;
                                    document.getElementById('meetingStartTimeM').disabled = false;
                                    document.getElementById('meetingEndTimeY').disabled = false;
                                    document.getElementById('meetingEndTimeM').disabled = false;
                                    document.getElementById('meetingContent').disabled = false;
                                    document.getElementById('peoples').disabled = false;
                                    $("#checkSave").show();
                                    document.getElementById('myModals').click();
                                } else {
                                    document.getElementById('meetingTitle').disabled = true;
                                    document.getElementById('meetingRoom').disabled = true;
                                    document.getElementById('meetingDateStartYMD').disabled = true;
                                    document.getElementById('meetingDateEndYMD').disabled = true;
                                    document.getElementById('meetingStartTimeY').disabled = true;
                                    document.getElementById('meetingStartTimeM').disabled = true;
                                    document.getElementById('meetingEndTimeY').disabled = true;
                                    document.getElementById('meetingEndTimeM').disabled = true;
                                    document.getElementById('meetingContent').disabled = true;
                                    document.getElementById('peoples').disabled = true;
                                    $("#checkSave").hide();
                                    document.getElementById('myModals').click();
                                }
                                
                            },
                            error: function(error){
                                console.log(error);
                            }
                        })
                    }
            });
            // 캘린더 랜더링
            calendar.render();
        });
    })();
//날짜변환
function dateFormat(date) {
    let month = date.getMonth() + 1;
    let day = date.getDate();
    let hour = date.getHours();
    let minute = date.getMinutes();
    let second = date.getSeconds();

    month = month >= 10 ? month : '0' + month;
    day = day >= 10 ? day : '0' + day;
    hour = hour >= 10 ? hour : '0' + hour;
    minute = minute >= 10 ? minute : '0' + minute;
    second = second >= 10 ? second : '0' + second;

    return date.getFullYear() + '-' + month + '-' + day + ' ' + hour + ':' + minute + ':' + second;
}
//예약일정 저장
function saveMeeting(){
    if(updateFlag ===0){ //저장
    let formData = $("#meetingForm").serialize();

    //1. DB에 입력 데이터 저장
    $.ajax({
            type: "POST",        
            url: "/event/reservation",
            data: formData,
            success: function(result){
                console.log(result);
               if(result.code==400){
                   alert(result.msg);
                   return ;
               } else if(result.code==200){
                    alert(result.msg);
                    window.location.href="http://localhost:3000/event/events";
               }
            },
            error: function(err){
                console.log(err);
            }
        });
    } else {
        updateReservation();
    }
    // calendar.addEvent({
    //     title: meetingTitle.value,
    //     start: meetingStart.value,
    //     end: meetingEnd.value,
    //     //allDay: arg.allDay
    //                 })
}

function updateReservation(){
    let formData = $("#meetingForm").serialize();
    console.log("수정~");
    //1. DB에 입력 데이터 저장
    $.ajax({
            type: "PUT",        
            url: "/event/reservation/"+id,
            data: formData,
            success: function(result){
                window.location.href="http://localhost:3000/event/events";
               
            },
            error: function(err){
                console.log(err);
            }
        });
}
$(document).ready(function(){
    let a = document.getElementById('userName').value;
    console.log(a);
    console.log(a=="");

    if(a=="") //손님이면

    $("#meetingStartTimeY option:eq(0)").prop("selected", true);
    $("#meetingStartTimeM option:eq(0)").prop("selected", true);
    $("#meetingEndTimeY option:eq(0)").prop("selected", true);
    $("#meetingEndTimeM option:eq(0)").prop("selected", true);
    $("#myModalDiv").hide();
    
})

</script>
</html>